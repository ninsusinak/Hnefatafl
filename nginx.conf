# nginx.conf

server {
    listen 80;
    server_name localhost;

    location / {
        # Redirect index.html to Hnefatafl.html
        rewrite ^/$ /Hnefatafl.html permanent;
    }

    # Additional configurations can go here if needed
}

